// Same palette as magikdex so a card looks like it belongs to the same family,
// but this app owns its own copy — the whole point of the split is that neither
// side imports from the other.
export const t = {
  base:    "#08090c",
  surface: "#12151a",
  accent:  "#38bdf8",
  white:   "#e8eaed",
  muted:   "#2a3138",
  border:  "#1b2129",
  dim:     "#5a6672",
  red:     "#e0555f",
};
